| Scaling issue                          | Current assumption                                                    | Code location                        | What breaks                                                               | Candidate fix                                          |
| -------------------------------------- | --------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------- | ------------------------------------------------------ |
| Larger fixed neuron count              | One `xdim` determines encoder input weights                           | `CPIC.py:643–652`, `models.py`       | Existing model cannot accept new `N`                                      | rebuild/configure input layer                          |
| Variable neuron counts across sessions | All samples in a batch have identical feature dimension               | `CPIC.py:630` / default `DataLoader` | default collation cannot stack `(T,N₁)` and `(T,N₂)`                      | padding + mask, custom batching, session grouping      |
| Shared encoder across variable `N`     | Encoder constructed using `X[0]`'s `N`                                | `CPIC.py:643–652`                    | even padded/custom batches don't inherently solve representation mismatch | adapters / set or token encoder / common feature space |
| Missing observations                   | Dense tensors assumed; no validity mask in training                   | training/encoder path                | NaNs propagate into activations/loss/gradients                            | mask-aware input + loss or explicit imputation         |
| Multiple sessions                      | `PastFutureDataset` accepts multiple time series and variable lengths | `utils/data.py`                      | works when feature dimension is homogeneous                               | preserve current abstraction                           |
| Variable sequence lengths              | Separate series may have different `T_i`                              | `utils/data.py`                      | already handled through window sampling                                   | no change                                              |
| Encoder depth                          | `n_layers` is configurable                                            | `models.py`, configs                 | none                                                                      | sweep                                                  |
| Hidden width                           | `hidden_dim` is configurable                                          | `CPIC.py`, configs                   | none                                                                      | sweep                                                  |
| Latent dimension                       | `ydim` is configurable                                                | `CPIC.py`, experiment loops          | none                                                                      | sweep `ydim`                                           |
| Different input/output dimensions      | Supported by `InputOutputPastFutureDataset`                           | `utils/data.py`                      | requires correctly sized critic                                           | already partially supported                            |
